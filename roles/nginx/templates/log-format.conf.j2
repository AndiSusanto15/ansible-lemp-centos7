log_format mylog '$remote_addr - $remote_user [$time_local] "$host" "$request" '
        '["$request_method $scheme://$host$request_uri $server_protocol"] '
        '$status $body_bytes_sent "$http_referer" '
        '"$http_user_agent" $request_time';